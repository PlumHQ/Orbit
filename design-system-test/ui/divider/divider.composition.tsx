import React from 'react';
import { Divider } from './component';

export const GrayLineSubtleDivider = () => {
  return <Divider stroke={'solid'} dividerStyle={'gray'} variant={'subtle'} />;
};

export const GrayLineNormalDivider = () => {
  return <Divider stroke={'solid'} dividerStyle={'gray'} variant={'normal'} />;
};

export const GrayLineHighlightedDivider = () => {
  return <Divider stroke={'solid'} dividerStyle={'gray'} variant={'highlighted'} />;
};

export const GrayDashedSubtleDivider = () => {
  return <Divider stroke={'dashed'} dividerStyle={'gray'} variant={'subtle'} />;
};

export const GrayDashedNormalDivider = () => {
  return <Divider stroke={'dashed'} dividerStyle={'gray'} variant={'normal'} />;
};

export const GrayDashedHighlightedDivider = () => {
  return <Divider stroke={'dashed'} dividerStyle={'beige'} variant={'highlighted'} />;
};

export const BeigeLineSubtleDivider = () => {
  return <Divider stroke={'solid'} dividerStyle={'beige'} variant={'subtle'} />;
};

export const BeigeLineNormalDivider = () => {
  return <Divider stroke={'solid'} dividerStyle={'beige'} variant={'normal'} />;
};

export const BeigeLineHighlightedDivider = () => {
  return <Divider stroke={'solid'} dividerStyle={'beige'} variant={'highlighted'} />;
};

export const BeigeDashedSubtleDivider = () => {
  return <Divider stroke={'dashed'} dividerStyle={'beige'} variant={'subtle'} />;
};

export const BeigeDashedNormalDivider = () => {
  return <Divider stroke={'dashed'} dividerStyle={'beige'} variant={'normal'} />;
};

export const BeigeDashedHighlightedDivider = () => {
  return <Divider stroke={'dashed'} dividerStyle={'gray'} variant={'highlighted'} />;
};
